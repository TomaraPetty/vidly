import React from 'react';

const Genres = (props) => {
//   const { genre } = props;

  return (
      
    // <ul className='list-group'>
    //   <li onClick={() => filterAction()} className='list-group-item'>{props.movies.genre.name}</li>
    // </ul>
    null
  );
};

export default Genres;
